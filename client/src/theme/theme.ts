import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#FFF3E0" },
  secondary: { main: "#FFCC80", contrastText: "#000000" }
};

export const theme = createMuiTheme({ palette });
